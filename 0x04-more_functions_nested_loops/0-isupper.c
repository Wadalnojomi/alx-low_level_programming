#include "main.h"

/**
  * _isdigit - If is 0 to 9 return 1, if not return 0, function check a digit
  *
  * @c: this is the entry
  *
  * Return: 0
  *
  **/
int _putchar(int c)

{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
