#include "main.h"

/**
 * add - adds two integers and returns the result
 * @y: int to be added to x
 * @x: int to be added to y
 * Return: sum of y and x
 */
int add(int y, int x)
{
	int z;

	z = y + x;

	return (z);
}
