#include "main.h"

/**
 * _isupper - prints if characters is upper case or not.
 * @c: character to be check.
 * 
 * Return: 1 if upper case else 0.
 */
int _isupper (int c)
{
	int n;

	if (n >= 'A' && n <= 'Z')
		return (1);
	else
		return (0);
}
